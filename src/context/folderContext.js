import React from 'react';

export  const folderContext =  React.createContext([]);
export const folderTasksContext = React.createContext([]);
export const folderInnerContext = React.createContext([]);